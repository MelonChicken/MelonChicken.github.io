---
interface Props {
  status: 'seed' | 'growing' | 'stable' | 'archived' | string;
  label?: string;
}

const { status, label = status } = Astro.props;
---

<span class:list={['badge', `st-${status}`]}><i class="d"></i>{label}</span>
